# 贡献指南

欢迎提交可复现的问题报告与针对明确问题的改进。提交前请先阅读 README 中的平台状态和 `docs/DEVELOPMENT.md`。

1. 在独立分支上修改，说明问题、行为变化和验证结果。
2. macOS 代码运行 `swift test`；Windows 代码在 `windows` 目录按锁文件还原、构建并测试。
3. 不提交 API Key、Token、私钥、描述文件、真实项目数据、用户媒体或本机绝对路径。复现材料使用合成数据。
4. 新增依赖或素材须提供来源与许可证；不要复制授权不明的 Skill、提示词库或代码。
5. Windows 仍为原型，请按当前实现范围描述改动，不将构建成功表述为可公开发行。

提交贡献意味着你有权提交该内容，并同意自有贡献按本仓库 MIT 许可证提供；第三方材料须保留其原有许可。安全问题请按 `SECURITY.md` 私下报告，不在公开问题中粘贴凭证或利用细节。
